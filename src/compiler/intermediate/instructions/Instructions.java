package compiler.intermediate.instructions;

import java.util.ArrayList;

/**
 * Extends from ArrayList&lt;Instruction&gt;. Represents a list of intermediate code instructions.
 * We use this instead of the ArrayList itself to provide some abstraction.
 */
public class Instructions extends ArrayList<Instruction> {
}
