package compiler.nodes.expressions;

import compiler.Compilation;

public class UnaryExpression extends CallExpression {
    public static Expression create(String operator, Expression expression, int line, int column, Compilation compilation) {
        return null;
    }
}
