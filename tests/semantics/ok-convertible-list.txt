// This test fails because we didn't implement inferred array initializers.
type s = structure { };
procedure main()
{
    a : list of float;
    a = [2, 3.2];
    b : list of s;
    b = [ null, new s()];
}